# @avenzo/config

Shared development configuration for the monorepo.

This package currently owns reusable TypeScript config files for Node, React, and React Native targets. Keep it small and focused on tooling configuration that benefits multiple workspaces.

Do not place runtime app logic, secrets, Prisma configuration, or environment-specific deployment behavior here.

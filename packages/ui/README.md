# @avenzo/ui

Light shared UI foundation for Avenzo clients.

Use this package for design tokens and truly reusable small primitives that apply across customer web, restaurant web, and mobile apps. Keep app-specific screens, flows, and large composed UI inside the app that owns them.

This package should stay free of backend runtime dependencies, database access, API secrets, and product-specific business logic.
